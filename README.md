# Cpp_Template-With-Optional-From-Callback
A simple example on how to check at compile time (using template metaprogramming) that a method receives a valid callback and that it returns a std::optional
